#include <stdio.h>
int main(){

    int diameter = 35;
    float radius = diameter/2;
    float pi = 3.14;
    float area = pi * (diameter/2) * (diameter/2) ;
    printf("Area of circle is:%.2f",area);

}